# 📚 1strag — RAG-чатбот по русским PDF

Локальный чатбот, который отвечает на вопросы по содержимому PDF-файлов на русском языке с указанием источников.

## 🎯 Особенности

- **Полностью локальный**: Ollama + FAISS, никаких облаков
- **Умный чанкинг**: Docling HybridChunker — не рвёт таблицы и списки
- **Качественные embeddings**: E5-base multilingual
- **Русский язык**: qwen2.5:14b — лучший открытый LLM для русского

---

## 🚀 Быстрый старт

### 1. Установка

```bash
pip install -r requirements.txt
```

### 2. Ollama

```bash
# Установка
curl -fsSL https://ollama.com/install.sh | sh

# Скачать модель
ollama pull qwen2.5:14b

# Запустить
ollama serve
```

### 3. Добавить PDF

Положите PDF файлы в папку `books/`.

### 4. Построить индекс

```bash
python rag_setup.py
```

### 5. Запустить

```bash
streamlit run app.py
```

---

## ⚙️ Конфигурация

| Переменная | Default | Описание |
|------------|---------|----------|
| `MAX_TOKENS` | 500 | Размер чанка (E5 лимит 512) |
| `TOP_K` | 20 | Количество чанков для retrieval |
| `OLLAMA_MODEL` | qwen2.5:14b | Модель LLM |

```bash
export TOP_K=30
python rag_setup.py
```

---

## 📁 Структура

```
1strag/
├── app.py              # Streamlit UI + RAG
├── rag_setup.py        # Сборка индекса
├── books/              # PDF файлы
└── rag_index/          # FAISS индекс
```

---

## 📖 Документация

- [ARCHITECTURE.md](ARCHITECTURE.md) — техническое описание
- [AntiOverengineering_RULES.md](AntiOverengineering_RULES.md) — принципы разработки

---

## 🔐 Безопасность

> ⚠️ **Не загружайте чужие `rag_index/`** — pickle-файлы могут содержать вредоносный код.

---

## 📝 Лицензия

MIT
